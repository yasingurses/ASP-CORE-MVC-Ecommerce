@using EntityLayer;
@model List<Tag>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Admin.cshtml";
}

<h1>ETİKET LİSTESİ</h1>
<div><a asp-action="Ekle" asp-controller="Tag" class="btn btn-primary">ETİKET EKLE</a></div>
<section class="section">
    <div class="row">
        <div class="col-lg-12">

            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">Default Table</h5>

                    <!-- Default Table -->
                    <table class="table">
                        <thead>
                            <tr>
                                <th scope="col">Etiket ID</th>
                                <th scope="col">Etiket Adı</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var a in Model)
                            {
                                <tr>
                                    <td>@a.tag_ID</td>
                                    <td>@a.tag_Name</td>
                                    <td>
                                        <a onclick="return confirm('Silmek istediğinize eminmisiniz?');" asp-controller="Tag" asp-action="Sil" asp-route-id="@a.tag_ID" class="btn btn-danger">sil</a>

                                        <a asp-controller="Tag" asp-action="Guncelle" asp-route-id="@a.tag_Name" class="btn btn-warning">Güncelle</a>
                                    </td>
                                    <td>
                                    </td>


                                </tr>
                            }

                        </tbody>
                    </table>
                    <!-- End Default Table Example -->
                </div>
            </div>

        </div>

    </div>
</section>

